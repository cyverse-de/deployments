apiVersion: skaffold/v3
kind: Config
metadata:
  name: clockwork
build:
  artifacts:
    - image: harbor.cyverse.org/de/clockwork
      custom:
        buildCommand: ./buildx-build.sh
        dependencies:
          paths: ["**"]
  platforms:
    - "linux/amd64"
  tagPolicy:
    gitCommit: {}
  local: {}
manifests:
  rawYaml:
    - k8s/clockwork.yml
deploy:
  kubectl: {}
