apiVersion: skaffold/v3
kind: Config
metadata:
  name: openldap
build:
  artifacts:
    - image: harbor.cyverse.org/de/openldap
      custom:
        buildCommand: ./buildx-build.sh
        dependencies:
          paths: ["**"]
  platforms:
    - "linux/amd64"
  tagPolicy:
    gitCommit: {}
  local: {}
manifests:
  rawYaml:
    - k8s/openldap-docker.yml
deploy:
  kubectl: {}
